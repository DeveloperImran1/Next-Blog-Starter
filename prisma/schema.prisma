
generator client {
  provider = "prisma-client-js"
  // output   = "../src/generated/prisma"   // ai line ke remove korte hobe.
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
